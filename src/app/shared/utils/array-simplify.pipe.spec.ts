import { ArraySimplifyPipe } from './array-simplify.pipe';

describe('ArraySimplifyPipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySimplifyPipe();
    expect(pipe).toBeTruthy();
  });
});
