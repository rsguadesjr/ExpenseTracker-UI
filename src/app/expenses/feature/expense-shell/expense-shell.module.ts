import { NgModule } from "@angular/core";
import { ExpenseShellRoutingModule } from "./expense-shell-routing.module";

@NgModule({
  imports: [
    ExpenseShellRoutingModule
  ]
})
export class ExpenseShellModule {}
